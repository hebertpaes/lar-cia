# Copie para .env e preencha. O .env NÃO vai para o Git (é secreto).
MYSQL_ROOT_PASSWORD=troque-por-uma-senha-forte

# SMTP para e-mails do Ghost (newsletter/login de membros). Ex.: Mailgun, SendGrid.
MAIL_SERVICE=Mailgun
MAIL_USER=postmaster@seu-dominio-de-email
MAIL_PASS=coloque-a-senha-smtp
